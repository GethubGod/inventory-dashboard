import { DashboardPagePlaceholder } from "@/components/layout/dashboard-page-placeholder";

export default function AppConnectionPage() {
  return (
    <DashboardPagePlaceholder
      title="Mobile App"
      description="Pair and monitor mobile app connections to keep field inventory updates synchronized in real time."
      comingSoon
    />
  );
}
