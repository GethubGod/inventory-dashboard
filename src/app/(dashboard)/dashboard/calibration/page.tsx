import { DashboardPagePlaceholder } from "@/components/layout/dashboard-page-placeholder";

export default function CalibrationPage() {
  return (
    <DashboardPagePlaceholder
      title="Calibration"
      description="Fine-tune prediction behavior with historical patterns, seasonality controls, and operational overrides."
      comingSoon
    />
  );
}
